# Todolist

一个轻量的纯前端待办事项项目，支持新增、编辑、删除、完成状态切换、筛选、统计和本地持久化。

## 功能

- 新增待办事项
- 编辑任务标题
- 标记任务完成或恢复为进行中
- 按全部、进行中、已完成筛选
- 清空已完成任务
- 使用 `localStorage` 自动保存

## 运行

直接在浏览器中打开 `index.html` 即可运行。

## 文件结构

```text
.
├── index.html
├── styles.css
├── app.js
└── README.md
```
